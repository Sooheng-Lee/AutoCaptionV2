# YouTube Subtitle Translator PRD

## 1. 제품 개요

### 1.1 제품명
YouTube Subtitle Translator

### 1.2 목적
사용자가 YouTube 영상 URL을 입력하면 영상을 분석하여 원어 자막 파일을 생성하고, 선택한 대상 언어로 번역된 자막 파일을 자동 생성하는 Python 기반 데스크톱 애플리케이션을 만든다.

### 1.3 핵심 가치
- YouTube 영상의 음성을 자동으로 자막화한다.
- 원어 자막과 번역 자막을 모두 파일로 제공한다.
- 긴 영상도 자동 분할 처리하여 안정적으로 번역한다.
- 필요한 음성인식 모델과 번역 모델이 없으면 자동 다운로드한다.
- 비개발자도 PyQt UI를 통해 쉽게 사용할 수 있다.

## 2. 대상 사용자

### 2.1 주요 사용자
- 외국어 YouTube 영상을 학습하거나 번역해야 하는 사용자
- 영상 콘텐츠를 자막 파일로 보관하고 싶은 사용자
- 강의, 인터뷰, 다큐멘터리 등 긴 영상의 자막을 번역해야 하는 사용자

### 2.2 사용자 목표
- YouTube URL만 입력해서 자막을 생성한다.
- 원어 자막과 번역 자막을 각각 다운로드한다.
- 필요하면 YouTube 영상 파일도 함께 다운로드한다.
- 긴 영상도 중간 실패 없이 처리한다.
- 모델 설치 상태를 몰라도 앱이 필요한 모델을 안내하고 준비한다.

## 3. 범위

### 3.1 포함 범위
- Python 기반 데스크톱 앱
- PyQt6 기반 GUI
- YouTube URL 입력
- 영상 메타데이터 조회
- YouTube 영상 및 자막 파일 다운로드
- 오디오 추출
- 오픈소스 음성인식 모델을 사용한 원어 자막 생성
- Gemma 4 모델을 사용한 자막 번역
- 음성인식 모델 및 Gemma 4 모델 자동 다운로드
- SRT 또는 VTT 자막 파일 생성
- 긴 자막 또는 긴 영상 자동 분할 처리
- 분할 번역 결과 병합
- 작업 진행률 표시
- 오류 메시지 표시

### 3.2 제외 범위
- 모바일 앱
- 웹 서비스 배포
- 실시간 스트리밍 번역
- 자막 편집기 수준의 세밀한 타임라인 편집
- 저작권 보호 영상 우회 다운로드

## 4. 주요 사용자 흐름

### 4.1 기본 자막 생성 흐름
1. 사용자가 앱을 실행한다.
2. YouTube 영상 URL을 입력한다.
3. 번역 대상 언어를 선택한다.
4. 자막 형식을 선택한다. 기본값은 `SRT`.
5. `자막 생성` 버튼을 클릭한다.
6. 앱이 YouTube 영상 정보를 확인한다.
7. 앱이 필요한 음성인식 모델과 Gemma 4 모델의 존재 여부를 확인한다.
8. 모델이 없으면 모델명, 예상 용량, 저장 위치를 표시하고 사용자 동의 후 다운로드한다.
9. 앱이 영상 또는 오디오를 다운로드한다.
10. 앱이 오디오에서 원어 자막을 생성한다.
11. 앱이 원어 자막을 Gemma 4 모델로 번역한다.
12. 앱이 원어 자막 파일과 번역 자막 파일을 저장한다.
13. 사용자는 결과 파일 위치를 확인하거나 다운로드 폴더를 연다.

### 4.2 영상 다운로드 흐름
1. 사용자가 YouTube URL을 입력한다.
2. `영상 다운로드` 옵션을 활성화한다.
3. 저장 경로와 품질을 선택한다.
4. 앱이 영상을 다운로드한다.
5. 다운로드 완료 후 파일 경로를 표시한다.

### 4.3 긴 영상 처리 흐름
1. 앱이 영상 길이 또는 원어 자막 토큰 수를 계산한다.
2. 기준치를 초과하면 자동으로 구간을 분할한다.
3. 각 구간별로 음성인식 또는 번역을 수행한다.
4. 구간별 자막의 타임코드를 원본 영상 기준으로 보정한다.
5. 모든 구간의 자막을 순서대로 병합한다.
6. 최종 원어 자막과 번역 자막을 생성한다.

## 5. 기능 요구사항

### 5.1 YouTube URL 입력
- 사용자는 YouTube 영상 URL을 입력할 수 있어야 한다.
- `youtube.com/watch?v=...`, `youtu.be/...` 형식을 지원한다.
- 잘못된 URL이면 즉시 오류 메시지를 표시한다.
- 재생목록 URL이 입력된 경우 기본적으로 단일 영상만 처리한다.

### 5.2 영상 정보 조회
- 앱은 다운로드 전에 영상 제목, 채널명, 영상 길이, 사용 가능한 자막 여부, 예상 처리 시간을 표시해야 한다.
- 영상 정보를 가져오지 못하면 작업을 중단하고 원인을 표시한다.

### 5.3 영상 및 오디오 다운로드
- YouTube 영상 다운로드 기능을 제공한다.
- 자막 생성만 필요한 경우 오디오만 다운로드할 수 있어야 한다.
- 다운로드 라이브러리는 `yt-dlp` 사용을 기본으로 한다.
- 다운로드 품질 옵션은 최고 품질, 1080p, 720p, 오디오만을 제공한다.
- 다운로드 파일명은 영상 제목 기반으로 생성하되, 파일 시스템에서 사용할 수 없는 문자는 제거한다.

### 5.4 모델 자동 다운로드 및 관리
- 앱은 작업 실행 전 필요한 모델의 존재 여부를 확인한다.
- 확인 대상은 음성인식 모델, Gemma 4 번역 모델, 모델 실행 런타임 또는 백엔드이다.
- 선택한 모델이 로컬에 없으면 다운로드 안내 모달을 표시한다.
- 자동 다운로드 전에는 모델명, 모델 종류, 예상 용량, 권장 사양, 라이선스 안내, 저장 위치를 사용자에게 표시한다.
- 사용자가 동의하면 앱이 모델을 다운로드한다.
- 다운로드 진행률, 속도, 남은 시간, 현재 파일명을 표시한다.
- 모델 다운로드는 백그라운드에서 수행하며 UI가 멈추지 않아야 한다.
- 다운로드된 모델은 기본 모델 저장 경로에 저장하고 이후 작업에서 재사용한다.
- 이미 다운로드된 모델은 중복 다운로드하지 않는다.
- 모델 버전 또는 파일 해시가 다른 경우 업데이트 여부를 사용자에게 묻는다.
- 다운로드 완료 후 체크섬 또는 해시 기반 무결성 검증을 수행한다.
- 무결성 검증에 실패하면 해당 모델을 다시 다운로드할 수 있어야 한다.
- 네트워크 오류나 다운로드 중단이 발생하면 이어받기 또는 재시도를 지원한다.
- 디스크 용량이 부족한 경우 다운로드 전에 경고한다.
- 자동 다운로드가 불가능한 환경을 위해 수동 모델 경로 지정 기능을 제공한다.
- 다운로드 취소 기능을 제공하며, 취소된 임시 파일은 보존 또는 삭제할 수 있어야 한다.

### 5.5 음성인식
- 오픈소스 음성인식 모델을 사용하여 원어 자막을 생성한다.
- 기본 후보 모델은 Whisper 계열 오픈소스 모델, `faster-whisper`, `whisper.cpp` 중 하나로 한다.
- 음성인식 결과는 타임코드가 포함된 세그먼트 형태여야 한다.
- 원어 자막 파일은 `SRT` 또는 `VTT`로 저장한다.
- 사용자는 음성인식 모델 크기를 선택할 수 있어야 한다.
  - 빠름
  - 균형
  - 정확도 우선
- GPU 사용 가능 여부를 감지하고, 가능하면 GPU 사용을 우선한다.
- 선택한 음성인식 모델이 로컬에 없는 경우 모델 자동 다운로드 흐름을 실행한다.
- 모델별 예상 용량, 권장 사양, 지원 언어를 표시해야 한다.

### 5.6 번역
- 원어 자막을 Gemma 4 모델에 입력하여 대상 언어로 번역한다.
- 번역 시 자막 번호와 타임코드는 변경하지 않는다.
- 번역 결과는 원어 자막과 동일한 세그먼트 구조를 유지해야 한다.
- 번역 대상 언어를 선택할 수 있어야 한다.
  - 한국어
  - 영어
  - 일본어
  - 중국어
  - 스페인어
  - 프랑스어
  - 독일어
  - 사용자 지정 언어
- 번역 프롬프트는 의미를 자연스럽게 번역하고, 자막 길이를 짧게 유지하며, 말투와 문맥을 보존해야 한다.
- 타임코드, 번호, 파일 형식 문법은 변경하지 않는다.
- Gemma 4 모델이 로컬에 없는 경우 모델 자동 다운로드 흐름을 실행한다.
- 자동 다운로드 전에는 모델명, 양자화 옵션, 예상 용량, 저장 위치를 사용자에게 표시한다.
- 사용자는 기본 추천 모델 또는 가벼운 모델을 선택할 수 있어야 한다.
- Gemma 4 실행 런타임이 필요한 경우 앱은 런타임 설치 여부를 감지하고 설정 화면에서 경로를 지정할 수 있게 한다.

### 5.7 긴 자막 분할 및 병합
- 원어 자막이 모델 입력 한계를 초과하거나 영상이 긴 경우 자동 분할한다.
- 분할 기준은 영상 길이, 자막 세그먼트 수, 예상 토큰 수, 모델 컨텍스트 윈도우를 조합하여 결정한다.
- 기본 분할 단위는 5분에서 10분 사이로 한다.
- 문장 또는 자막 세그먼트 중간에서 자르지 않는다.
- 번역 품질을 위해 인접 구간의 일부 문맥을 오버랩할 수 있다.
- 오버랩 문맥은 최종 출력에 중복 병합하지 않는다.
- 병합 시 자막 번호 재정렬, 타임코드 순서 보존, 중복 세그먼트 제거, 누락 세그먼트 검증을 보장한다.

### 5.8 자막 파일 생성
- 원어 자막 파일과 번역 자막 파일을 각각 생성한다.
- 기본 저장 형식은 `SRT`로 한다.
- 선택적으로 `VTT`도 지원한다.
- 파일명 규칙은 다음과 같다.
  - 원어 자막: `{video_title}.source.srt`
  - 번역 자막: `{video_title}.{target_language}.srt`
- 저장 완료 후 파일 경로를 UI에 표시한다.

### 5.9 진행률 표시
- 앱은 각 단계별 진행률을 표시해야 한다.
  - 영상 정보 조회
  - 모델 확인
  - 모델 다운로드
  - 영상 또는 오디오 다운로드
  - 오디오 추출
  - 음성인식
  - 번역
  - 병합
  - 파일 저장
- 긴 작업 중 UI가 멈추지 않아야 한다.
- 백그라운드 워커 스레드 또는 작업 큐를 사용한다.

### 5.10 오류 처리
- 네트워크 오류 발생 시 재시도 옵션을 제공한다.
- YouTube 다운로드 실패 시 오류 원인을 표시한다.
- 모델 다운로드 실패 시 실패 원인, 재시도 버튼, 수동 모델 경로 지정 옵션을 표시한다.
- 모델 파일이 손상되었거나 무결성 검증에 실패하면 해당 모델을 다시 다운로드할 수 있어야 한다.
- 음성인식 실패 시 해당 단계에서 중단하고 임시 파일을 보존할지 선택하게 한다.
- 번역 실패 시 실패한 구간만 재시도할 수 있어야 한다.
- 디스크 용량 부족, 권한 오류, 잘못된 저장 경로를 명확히 표시한다.

## 6. 비기능 요구사항

### 6.1 성능
- 10분 영상 기준으로 사용자가 처리 진행 상황을 확인할 수 있어야 한다.
- 긴 영상은 메모리 전체 로딩을 피하고 구간 단위로 처리한다.
- 번역 요청은 구간 단위로 캐싱하여 실패 시 전체 작업을 다시 하지 않도록 한다.

### 6.2 안정성
- 작업 중 앱을 종료하면 임시 작업 상태를 저장한다.
- 동일 URL을 다시 처리할 때 기존 다운로드 파일을 재사용할 수 있어야 한다.
- 각 단계 결과물을 임시 디렉터리에 저장하여 재시도 가능하게 한다.
- 모델 다운로드 중 앱이 종료되면 다음 실행 시 이어받기 또는 새로 다운로드를 선택할 수 있어야 한다.

### 6.3 보안 및 개인정보
- 사용자의 YouTube URL, 자막, 다운로드 파일은 기본적으로 로컬에만 저장한다.
- 외부 API를 사용하는 경우 사용자의 명시적 설정이 필요하다.
- 로컬 Gemma 4 모델 사용을 기본 전제로 한다.
- 모델 다운로드 출처는 신뢰 가능한 공식 또는 사전 설정된 저장소로 제한한다.
- 다운로드한 모델 파일은 해시 또는 체크섬으로 검증한다.
- 로그에는 민감한 경로나 사용자 입력 전체를 과도하게 저장하지 않는다.

### 6.4 호환성
- Windows를 1차 지원 대상으로 한다.
- Python 3.11 이상을 권장한다.
- PyQt6 사용을 기본으로 한다.
- GPU가 없어도 CPU 모드로 동작해야 한다.

## 7. UI 요구사항

### 7.1 메인 화면
- YouTube URL 입력 필드
- URL 분석 버튼
- 번역 대상 언어 선택 콤보박스
- 자막 형식 선택
- 음성인식 모델 선택
- Gemma 4 모델 선택
- 영상 다운로드 여부 체크박스
- 저장 경로 선택 버튼
- 자막 생성 버튼
- 모델 확인 및 다운로드 상태 표시 영역
- 작업 진행률 바
- 로그 패널
- 결과 파일 목록

### 7.2 결과 영역
- 원어 자막 파일 열기
- 번역 자막 파일 열기
- 영상 파일 열기
- 저장 폴더 열기
- 실패 구간 재시도 버튼

### 7.3 설정 화면
- 기본 저장 경로
- 모델 저장 경로
- 기본 번역 언어
- 음성인식 모델 경로
- Gemma 4 모델 경로
- 모델 자동 다운로드 사용 여부
- 모델 다운로드 전 확인 여부
- GPU 사용 여부
- 분할 기준 길이
- 최대 토큰 수
- 임시 파일 보존 여부

## 8. 시스템 아키텍처

### 8.1 주요 모듈
- `ui`: PyQt 화면, 이벤트 처리, 상태 표시
- `youtube`: URL 검증, 메타데이터 조회, 영상 및 오디오 다운로드
- `audio`: 오디오 추출, 포맷 변환, 구간 분할
- `model_manager`: 모델 존재 여부 확인, 자동 다운로드, 무결성 검증, 경로 관리
- `transcription`: 음성인식 모델 실행, 원어 자막 생성
- `translation`: Gemma 4 모델 호출, 번역 자막 생성
- `subtitle`: SRT/VTT 파싱, 생성, 병합, 검증
- `job`: 백그라운드 작업 관리, 진행률 이벤트, 재시도
- `config`: 사용자 설정 저장 및 로드
- `storage`: 파일명 정리, 임시 파일 관리, 결과 저장

### 8.2 처리 파이프라인
1. URL 검증
2. 영상 메타데이터 조회
3. 필요한 모델 존재 여부 확인
4. 누락된 모델 자동 다운로드 또는 사용자 확인
5. 오디오 또는 영상 다운로드
6. 오디오 정규화
7. 음성인식 수행
8. 원어 SRT 생성
9. 자막 분할 여부 판단
10. Gemma 4 번역 수행
11. 번역 결과 검증
12. 분할 결과 병합
13. 최종 SRT/VTT 저장

## 9. 데이터 및 파일 구조

### 9.1 작업 디렉터리 예시
```text
outputs/
  video-title/
    video.mp4
    audio.wav
    source.srt
    ko.srt
    job.json
    chunks/
      chunk_001.source.srt
      chunk_001.ko.srt
      chunk_002.source.srt
      chunk_002.ko.srt
models/
  speech/
  gemma4/
```

### 9.2 작업 상태 파일
`job.json`에는 다음 정보를 저장한다.
- YouTube URL
- 영상 ID
- 영상 제목
- 대상 언어
- 사용한 음성인식 모델
- 사용한 Gemma 4 모델
- 현재 단계
- 완료된 청크 목록
- 실패한 청크 목록
- 결과 파일 경로
- 생성 시각

## 10. 모델 요구사항

### 10.1 음성인식 모델
- 오픈소스 모델이어야 한다.
- 로컬 실행을 지원해야 한다.
- 세그먼트별 시작 시간과 종료 시간을 반환해야 한다.
- 모델 크기별 성능 옵션을 제공해야 한다.
- 앱에서 자동 다운로드 가능한 모델 목록을 제공해야 한다.
- 모델별 예상 용량, 권장 사양, 지원 언어를 표시해야 한다.

### 10.2 Gemma 4 번역 모델
- 로컬 실행을 우선 지원한다.
- 모델 경로 또는 실행 엔진을 설정할 수 있어야 한다.
- 긴 입력을 처리할 수 없는 경우 앱의 분할 로직으로 보완한다.
- 번역 출력은 원본 자막 구조와 개수가 가능한 한 일치해야 한다.
- 앱에서 자동 다운로드 가능한 Gemma 4 모델 목록을 제공해야 한다.
- 모델별 예상 용량, 양자화 방식, 권장 메모리, 권장 실행 런타임을 표시해야 한다.
- 자동 다운로드가 불가능한 환경을 위해 수동 모델 경로 지정 기능을 제공해야 한다.

## 11. 번역 프롬프트 요구사항

### 11.1 기본 프롬프트
```text
You are a professional subtitle translator.
Translate the subtitle text into {target_language}.
Preserve subtitle numbering and timestamps exactly.
Do not merge, split, remove, or reorder subtitle blocks.
Keep the translation concise and natural for subtitles.
Return only valid {subtitle_format} content.
```

### 11.2 검증 규칙
- 입력 자막 블록 수와 출력 자막 블록 수를 비교한다.
- 타임코드가 변경되었는지 확인한다.
- 빈 번역이 있는지 확인한다.
- 형식이 깨진 경우 같은 청크를 재번역한다.

## 12. MVP 범위

### 12.1 MVP 필수 기능
- PyQt6 메인 UI
- YouTube URL 입력
- 누락된 모델 자동 다운로드
- 오디오 다운로드
- 오픈소스 음성인식 모델 연동
- 원어 SRT 생성
- Gemma 4 번역 연동
- 번역 SRT 생성
- 긴 자막 청크 분할 및 병합
- 진행률 표시
- 결과 파일 저장

### 12.2 MVP 이후 기능
- 영상 파일 다운로드 품질 선택
- VTT 지원
- 자막 미리보기
- 실패 청크 개별 재시도
- 작업 이력
- 기존 자막 파일 불러오기 후 번역
- 다중 영상 배치 처리

## 13. 성공 지표

- 사용자가 URL 입력 후 원어 자막과 번역 자막을 생성할 수 있다.
- 음성인식 모델 또는 Gemma 4 모델이 없는 환경에서도 앱이 모델을 자동 다운로드하여 작업을 계속할 수 있다.
- 1시간 이상의 긴 영상도 분할 번역 후 하나의 자막 파일로 병합된다.
- 번역 자막의 타임코드가 원어 자막과 동일하게 유지된다.
- 작업 중 UI가 멈추지 않는다.
- 실패 시 사용자가 원인을 이해하고 재시도할 수 있다.

## 14. 주요 리스크 및 대응

### 14.1 YouTube 다운로드 실패
- `yt-dlp` 업데이트 안내를 제공한다.
- 다운로드 실패 로그를 표시한다.
- 오디오만 다운로드하는 대체 경로를 제공한다.

### 14.2 음성인식 정확도 저하
- 모델 크기 선택 옵션을 제공한다.
- 오디오 정규화와 샘플레이트 변환을 수행한다.
- 언어 자동 감지와 수동 언어 선택을 모두 지원한다.

### 14.3 번역 출력 형식 깨짐
- SRT 파서를 통해 출력 형식을 검증한다.
- 실패 청크만 재번역한다.
- 타임코드는 번역 모델 출력 대신 원본 자막 기준으로 재적용한다.

### 14.4 긴 영상 처리 실패
- 영상과 자막을 청크 단위로 처리한다.
- 각 청크 결과를 디스크에 저장한다.
- 중단된 작업은 마지막 성공 청크부터 재개한다.

### 14.5 모델 다운로드 실패
- 네트워크 상태와 저장 공간을 사전에 확인한다.
- 다운로드 실패 시 이어받기와 처음부터 다시 다운로드를 모두 지원한다.
- 자동 다운로드가 반복 실패하면 사용자가 직접 모델 경로를 지정할 수 있게 한다.
- 모델 출처 URL, 파일명, 체크섬 검증 결과를 로그에 남긴다.

## 15. 개발 마일스톤

### Milestone 1: 기본 UI 및 프로젝트 구조
- PyQt6 앱 초기 구조 생성
- URL 입력 및 설정 UI 구현
- 작업 로그와 진행률 표시 구현

### Milestone 2: 모델 관리자
- 모델 존재 여부 확인 구현
- 음성인식 모델 자동 다운로드 구현
- Gemma 4 모델 자동 다운로드 구현
- 모델 체크섬 검증 및 수동 경로 지정 구현

### Milestone 3: YouTube 다운로드
- URL 검증 구현
- 영상 메타데이터 조회 구현
- 오디오 다운로드 구현
- 영상 다운로드 옵션 구현

### Milestone 4: 원어 자막 생성
- 음성인식 모델 연동
- 세그먼트 추출
- SRT 생성
- 긴 영상 오디오 분할 처리

### Milestone 5: 번역 자막 생성
- Gemma 4 모델 연동
- 자막 청크 분할
- 번역 프롬프트 적용
- 번역 결과 검증

### Milestone 6: 병합 및 재시도
- 청크별 자막 병합
- 실패 청크 재시도
- 작업 상태 저장
- 최종 결과 파일 생성

### Milestone 7: 안정화
- 예외 처리 강화
- 긴 영상 테스트
- UI 멈춤 방지 검증
- 패키징 준비

## 16. 오픈 이슈

- Gemma 4 모델 실행 방식 확정 필요
  - 로컬 런타임
  - Ollama
  - llama.cpp 계열 런타임
  - 별도 API 서버
- 기본 음성인식 모델 확정 필요
- 음성인식 모델과 Gemma 4 모델의 공식 다운로드 출처 확정 필요
- 모델별 체크섬 또는 무결성 검증 방식 확정 필요
- 모델 자동 다운로드 시 사용자 동의 문구와 라이선스 고지 방식 확정 필요
- 지원할 최소 GPU/CPU 사양 정의 필요
- 다국어 번역 품질 평가 기준 정의 필요
- YouTube 다운로드 관련 이용 약관 고지 문구 필요
